spring:
  application:
    name: gulimall-ware
  datasource:
    username: root
    password: 4124894
    url: jdbc:mysql://172.16.212.10:3306/gulimall_wms
    driver-class-name: com.mysql.cj.jdbc.Driver
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848

mybatis-plus:
  # classpath*表示包括依赖包的类路径，不加*表示只扫描自己的
  mapper-locations: classpath*:/mapper/**/*.xml
  global-config:
    db-config:
      # 配置主键自增
      id-type: auto

server:
  port: 11000
